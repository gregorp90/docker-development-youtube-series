# ################################################################
# # This source reads tail of a file
# @include file-fluent.conf
#
# ################################################################
#
# # This source gets incoming logs over HTTP
# @include http-fluent.conf
#
# ################################################################
# # This source gets all logs from local docker host
# @include containers-fluent.conf
#
# ################################################################
# # Send all logs to elastic search
# @include elastic-fluent.conf
# <source>
#   @type forward
#   port 24224
# </source>
#
# <match fluentd.test.**>
#   @type file
#   path /output/python-app.log
# </match>

<source>
  @type forward
  port 24224
</source>
# <match fluentd.test.**>
#   # @type stdout
#   @type file
#   path /output/python-app.log
# </match>

#where to send file logs
<match fluentd.test.**>
  @type elasticsearch
  host elasticsearch
  port 9200
  verify_es_version_at_startup false
  default_elasticsearch_version 7
  index_name fluentd-python
  type_name fluentd
</match>